# Generate a long random value and add it in Hostinger's Environment Variables.
CONTACT_EXPORT_TOKEN=replace-with-at-least-24-random-characters

# Optional. Defaults to <application-root>/server.
# Use an absolute writable directory if you want data outside the deployment folder.
CONTACT_DATA_DIR=./server
